Two more items needed to unblock the pricing and classification work. First, please provide the hostname and port for both the US gateway and the global gateway. Second, please provide the full four-rate pricing table, input tokens, output tokens, cache-read tokens, and cache-write tokens, for every model tested so far, with the currency and the per-unit basis stated explicitly for each rate, for example whether it's per-million-tokens or per-thousand-tokens. Please don't assume a default unit, state it plainly for every figure, since an unstated unit would silently throw off the pricing table by a factor of a thousand.
