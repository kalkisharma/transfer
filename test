The one thing I keep coming back to: US Claude reported zero cache tokens on two identical requests, so unlike GPT-OSS-120b, which at least fails visibly with a fourth oh four, US Claude's caching just looks like it silently isn't happening. Worth asking the other session whether that's confirmed as caching genuinely not supported on that path, or whether it's a test-setup issue, wrong header, cache control not enabled, something that could be retested with a deliberate cache-eligible payload before we tell the coordinator it doesn't work there.
