Now that the model attribution is confirmed, please capture and save the raw request and response bytes for the US native Anthropic cache test, for both Opus and Sonnet since both were run independently. For each model, that's the warm call and the changed-suffix cache-read call, byte-exact, no normalization, same standard as prior captures. Since both produced identical 8,823 written then 8,823 read figures, having both real captures on file removes any future ambiguity about which model a given fixture belongs to.
