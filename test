Two follow-ups. First, the raw-bytes capture for the real cache pair was only done on US native Anthropic — do we need the same warm-plus-changed-suffix capture on global native Anthropic as well, for both Opus and Sonnet, or is US sufficient given native Anthropic likely behaves consistently across regions? Let us know if you'd recommend capturing global too for completeness. Second, the new capture files are currently untracked in the working directory, which the git-index verifier flagged as blocking full validation — should these be staged or committed now, or is that a step we handle on our end?
