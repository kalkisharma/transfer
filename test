Please capture the cache-warm plus changed-suffix pair for global native Anthropic, covering both Opus and Sonnet, the same recipe already used for US native Anthropic. This completes proof that prefix-caching works on both endpoints, not just US. This is a one-time validation, save the raw request and response captures the same way as before, triplet format with request pretty text, response pretty text, and capture json. No repeated runs needed once this is done.
