One clarification needed before we finalize cost handling. What actually marks a model as on-prem versus externally hosted in the gateway catalog or config, is it an explicit field, a naming convention, something in the pricing page, or is that distinction not exposed anywhere and we've just been inferring it from gpt-oss-120b being open-weight? We want to rule that gpt-oss-120b reports no cost as a property of that specific model, but need to know if there's a real signal to key that off of, or if it needs to be a manually maintained list.
