One more capture needed. For the same request that already produced a genuine cache read — Sonnet on the US native Anthropic route, cache-warm followed by an identical replay — please capture and save the raw request and response bytes for both calls (warm and replay), not just the token counts. We have the numbers (8,823 created, 8,823 read back) but not the underlying bodies, and those are needed to build a verifiable fixture. Same standard as prior captures: byte-exact, no normalization.
