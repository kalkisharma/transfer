Two questions. First, in the US Claude cache test that reported zero, is that an actual reported zero value in the cache fields, or are the cache fields completely absent from the response? Second, why is claude-opus-4-8 available through the OpenAI-compatible gateway at all, rather than only the native Anthropic gateway, is there documented reasoning for offering both paths for the same model?
