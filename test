In the companion pretty-printed files, please make three changes. First, wrap or line-break any long HTTP header lines so they stay within a reasonable terminal width, even if it breaks strict HTTP formatting, since this is just for readability. Second, replace the actual content of the text field with a stand-in showing character count instead, something like text field, colon, redacted, comma, character count. Third, do the same for the instructions field, same stand-in format showing character count instead of the real content.
