So, three things. First, can you run the same controlled warm-and-reuse caching test on global Terra that was done for global Claude, so we get a confirmed cache-creation and cache-read count there too. Second, for GPT-OSS-120b's tool-call continuation failure, can you get the actual error message or response body when the prior response isn't found, so we know if it's a missing ID, an expired session, or something else entirely. And third, since we now have exact model strings for three models, are there more models on either gateway worth naming now, or are Terra, GPT-OSS-120b, and Claude Opus four eight the complete set that matter for this handoff?
